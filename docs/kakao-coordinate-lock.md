# Kakao 좌표 고정값 (절대좌표)

이 파일은 카카오 자동수집 좌표의 **단일 기준 원본**이다.
앞으로 좌표가 꼬이거나 누락되면, 이 파일 값을 우선 로드해서 코드에 반영한다.

## 1) 채팅방 진입 좌표 (오픈채팅 목록)
- room1: `(237,126)`
- room2: `(237,205)`
- room3: `(228,269)`
- room4: `(231,344)`
- room5: `(231,417)`  # 계산 확장
- room6: `(231,490)`  # 계산 확장
- room7: `(231,563)`  # 계산 확장
- room8: `(231,636)`  # 계산 확장

## 2) 채팅방 내 저장 플로우 좌표
- 햄버거/설정: `(762,91)`
- 대화내용관리: `(845,157)`
- 텍스트 파일로 저장: `(1000,191)`
- 저장: `(1206,329)`
- 완료: `(1075,323)`

## 3) 적용 원칙
1. 좌표계는 절대좌표만 사용한다.
2. 임의 보정(캔버스 변환, 자동 오프셋) 금지.
3. 좌표 변경 시 이 파일 먼저 수정하고, 스크립트 반영한다.
4. 문제 발생 시 이 파일과 코드 값 diff를 먼저 확인한다.

## 4) 코드 반영 대상
- `scripts/kakao_alert_only_export_loop.sh`
- `scripts/detect_badge_rows.py` (행 앵커 Y)
